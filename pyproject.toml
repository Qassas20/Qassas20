[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "trading-bot-calibration"
version = "0.1.0"
description = "Reality Calibration Layer for the multi-bucket trading bot — makes Alpaca paper trading a valid predictor of live performance."
readme = "docs/CALIBRATION.md"
requires-python = ">=3.9"
license = { text = "MIT" }
dependencies = []   # stdlib only (sqlite3, urllib, subprocess, dataclasses)

[project.optional-dependencies]
dev = ["pytest"]
# Only needed for the real backend Claude Market-Intelligence call; the rest of
# the package (and all tests) run without it.
claude = ["anthropic"]

[tool.setuptools]
packages = [
    "trading_bot",
    "trading_bot.calibration",
    "trading_bot.db",
    "trading_bot.execution",
    "trading_bot.reporting",
    "trading_bot.broker",
    "trading_bot.signals",
    "trading_bot.intelligence",
    "trading_bot.orchestration",
    "trading_bot.backtest",
]

[tool.setuptools.package-data]
"trading_bot.db" = ["schema.sql"]

[tool.pytest.ini_options]
testpaths = ["tests"]
